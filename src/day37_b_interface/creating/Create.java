package day37_b_interface.creating;

public interface Create {
//by default the following method is public abstract we do not need to declare it with public abstract
    public void read(); //abstract method by default

    void write();
}
